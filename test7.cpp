#include "header/std_lib_facilities.h"

using namespace std;

int main()
{
    char alpha;

    alpha = 'a';

    while (alpha <= 'z')
    {
        cout<<"the ans is: "<<int(alpha)<<endl;
        alpha++;
    }
    return 0;
}
