#include "../header/std_lib_facilities.h"

using namespace std;

int main()
{
    vector<int>x = {2, 3, 4, 5, 6};

    int i;

    i = 0;
    while (i < x[i])
    {
        cout<<"just testing vector: "<<x[i]<<'\n';
        i++;
    }
    return(0);
}
